SHELL:=/bin/bash

#---------------------------------

LOGGER=./log.sh
LOG_INFO=$(LOGGER) INFO
LOG_WARN=$(LOGGER) WARN

#---------------------------------

data_stem = $(subst *,all,$(if $(filter %.table, $1),$(basename $(notdir $1)),$(notdir $1)))

#------------ create directories -----------

RUN_DIR=tte.run_dir

include makefile.config

$(RUN_DIR) :
	mkdir $@

